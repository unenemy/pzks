= form_tag root_path do
  = text_field_tag :expression, @expression
  = submit_tag "PROCESS"
%b Parsed result:
%p= @parsed
%p= @message
%br
%b Оптимизация соседних - и /
%p= @optimized
%b Дерево:
%p
  - @pre_tree.each do |node|
    %p=node
%b Дерево:
%p
  - @tree.each do |node|
    %p=node
